# Name of an S3 bucket you have read/write access to
bucket = "myterraformstate.mywebsite.com"

# Region the bucket is in
region = "us-east-1"

# Key of the bucket object which will store the terraform state
key = "applications/terraform-node-pm2/deployments/dev/terraform.tfstate"
